import createItemBag from "../createItemBag";
import { jar as img } from "../../../img/item";

const jar = createItemBag(
    "Jar",
    50,
    "gatcha",
    `One jar to capture them all, and in the darkness bind them!`,
    img
);

export default jar;